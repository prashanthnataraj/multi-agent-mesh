# Real bot tokens + allowlist — never commit
secrets/bots.json
secrets/access.json

# Local environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/
bun.lock
bun.lockb
package-lock.json

# Build outputs
dist/
build/

# Logs
*.log
poller.log

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Local poller state (per-machine; lives in ~/.claude/agent-mesh/ but
# guard against accidentally committing if a user copies it into the repo)
last-update.json
poller.lock
inbox/
dedup.json
