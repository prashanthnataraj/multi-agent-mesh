{
  "_comment": "Copy this file to secrets/access.json and fill in real values. The poller drops every message that doesn't pass this allowlist — so a missing or empty file means a quiet inbox.",
  "_doc": "See docs/telegram-setup.md steps 6 and 7 for how to find your group chat_id and your own user_id.",
  "allowedUsers": [
    "REPLACE_WITH_YOUR_NUMERIC_TELEGRAM_USER_ID"
  ],
  "allowedGroups": [
    "REPLACE_WITH_YOUR_GROUP_CHAT_ID_PROBABLY_STARTS_WITH_-100"
  ]
}
